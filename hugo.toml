# ===================== 基础核心配置 =====================
baseURL = "https://noctis-sikong.github.io"
languageCode = "zh-cn"
title = "一切都是命运石之门的选择！"
theme = "github-style"
summaryLength = 10
enableSearch = true  # 全局开启搜索

# 关键：配置搜索索引的输出格式（生成search.json）
[outputFormats.SearchJSON]
baseName = "search"          # 输出文件名为 search.json
mediaType = "application/json"
isPlainText = true
notAlternative = true

# 修正home页面的输出类型（使用自定义的SearchJSON）
[outputs]
home = ["HTML", "RSS", "SearchJSON"]  # 替换原来的JSON为SearchJSON

# ===================== 分类法配置（新增专栏核心） =====================
[taxonomies]
  series = "series"    # 启用「专栏」功能（核心）
  category = "category"# 保留分类（可选，不用可删）
  tag = "tag"          # 保留标签（可选，不用可删）

# ===================== 个性化参数 =====================
[params]
  # 基础个人信息
  avatar = "/克里斯.jpg"        
  author = "天才变态少女"   
  location = "东京秋叶原"        
  userStatusEmoji = "😎"        
  github = "Noctis-SiKong"
  twitter = "almosy"
  email = "hui703101@163.com"        
  favicon = "/R-C.png"      
  headerIcon = "/R-C.png"    
  custom_css = ["css/custom.css"]  
  custom_js = ["js/custom.js"]    
  lastmod = true                
  enableGitalk = true  
  rss = true                    

  # 搜索功能自定义
  [params.search]
    fuseJs = true  
    maxResultLength = 10  
    threshold = 0.6      

  # 专栏功能配置（新增）
  [params.series]
    enable = true        # 开启专栏功能
    title = "我的专栏"   # 专栏页面标题
    showCount = true     # 显示每个专栏的文章数量

# ===================== 最后修改时间配置 =====================
[frontmatter]
lastmod = ["lastmod", ":fileModTime", ":default"]

# ===================== Gitalk评论配置（修正clientSecret冒号错误） =====================
[params.gitalk]
  clientID = "Ov23liJPJ1rIHChpsuKV"  
  clientSecret = "d73a8d88462d1ea39e6765fe614ad2f85f586622"  # 已删除多余冒号，避免评论报错
  repo = "blog-comments"  
  owner = "Noctis-SiKong"  
  admin = ["Noctis-SiKong"]