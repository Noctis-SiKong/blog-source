---
title: '卷积核'
date: '2025-12-12T21:35:10+08:00'
draft: false
series: '深度学习'
---



# 深度学习：卷积神经网络中的卷积核核心知识总结

卷积核是 CNN 实现图像特征提取的核心组件，其设计与运算直接决定网络性能。本文将浓缩卷积核的本质、特性、运算原理及 1x1 特殊卷积核的核心作用。

<!--more-->

## 一、卷积核的本质与核心作用

卷积核（又称滤波器）是图像处理中用于提取特征的核心工具，其本质是对输入图像的局部区域（感受野）进行像素加权平均运算，权值由预设函数定义。在卷积神经网络（CNN）中，卷积核承担着 "前排特征提取" 的关键角色，CNN 的核心逻辑就是通过卷积核不断提取图像特征、进行特征选择，最终实现分类任务，本质上是计算图像局部与卷积核的互相关函数。

## 二、卷积核的关键特性

1. **特征提取的特异性**：
   - 不同卷积核可提取图像的不同特征（如边缘、纹理、轮廓等）；
   - 即使目标特征相同，不同规格的卷积核（如 3x3 与 5x5）提取效果也存在差异（例：5x5 锐化卷积核的效果比 3x3 更细腻）。
2. **尺寸规格**：
   - 常用尺寸为 1x1、3x3、5x5，通常采用奇数 × 奇数设计；
   - 卷积核尺寸直接影响特征提取的粒度，小尺寸核侧重局部细节，大尺寸核侧重更广泛的特征关联。
3. **维度匹配规则**：
   - 卷积核的输入通道数（in depth）由输入图像的通道数决定（如 RGB 三通道图像需对应 3 通道卷积核）；
   - 每个卷积核会将所有输入通道压缩为单个输出通道，输出特征图（feature map）的数量等于卷积核的个数。

## 三、卷积运算的核心原理

### 1. 运算过程

以输入 224x224x3（RGB 三通道）、输出 32 位深度、卷积核尺寸 5x5 为例：

- 需配置 32 个 5x5x3 规格的卷积核（最后一维与输入通道数匹配）；
- 每个卷积核的 3 层（5x5）分别与输入图像的对应通道卷积；
- 将 3 次卷积结果算术求和，得到 1 张深度为 1 的 feature map；
- 32 个卷积核分别运算后，最终输出 32 张不同的 feature map。

### 2. 核心公式

*a**i*,*j*=*f*(∑*d*=0*D*−1∑*m*=0*F*−1∑*n*=0*F*−1*w**d*,*m*,*n**x**d*,*i*+*m*,*j*+*n*+*w**b*)

- 符号说明：D 为输入深度，F 为卷积核尺寸，*w**d*,*m*,*n*为卷积核第 d 层第 m 行第 n 列权重，*x**d*,*i*+*j*,*j*+*n*为图像第 d 层第 i 行第 j 列像素，*w**b*为偏置项，f 为激活函数。

### 3. 输入输出尺寸计算

- 输入矩阵格式：样本数 × 图像高度 × 图像宽度 × 通道数；

- 输出矩阵格式：样本数 × 输出高度 × 输出宽度 × 输出通道数（= 卷积核个数）；

- 尺寸计算公式（忽略偏置项）：

  

  {*h**e**i**g**h**t**o**u**t**w**i**d**t**h**o**u**t*=(*h**e**i**g**h**t**in*−*h**e**i**g**h**t**k**er**n**e**l*+2×*p**a**dd**in**g*)÷*s**t**r**i**d**e*+1=(*w**i**d**t**h**in*−*w**i**d**t**h**k**er**n**e**l*+2×*p**a**dd**in**g*)÷*s**t**r**i**d**e*+1

- 关键参数：stride（步长）为卷积核滑动间隔，padding（填充）为输入图像边缘补充像素数。

## 四、特殊卷积核：1x1 卷积核

### 1. 本质定位

1x1 卷积核又称 "网中网（Network in Network）"，可看作一种跨通道的池化操作或简化的全连接层，核心特点是不改变输出特征图的宽度和高度，仅调整通道数。

### 2. 核心作用

- **降维 / 升维**：当卷积核个数小于输入通道数时实现降维（如 6x6x32 输入经 1x1x32 卷积核输出 6x6x1），大于输入通道数时实现升维，本质是调整 height×width×channels 中通道维度的大小；
- **增加非线性**：在保持特征图分辨率不变的前提下，通过后接的非线性激活函数（如 ReLU）大幅提升网络的非线性表达能力，支持构建更深的网络；
- **跨通道信息交互**：实现不同通道间的线性组合，例如将 64 通道通过 1x1 卷积核线性组合为 28 通道，完成通道间的信息融合（仅在通道维度运算，宽高维度共享权值滑动窗口）。

## 五、补充：池化与卷积的关联

池化（pooling）是卷积后的特征聚合操作，用于获取全局特征，与卷积形成互补：

- 作用：对单个卷积通道的局部特征进行全局聚合，实现特征降维或升维，增强特征的全局性；
- 与 1x1 卷积的关联：1x1 卷积可看作跨通道的池化操作，二者均具备维度调整功能。

> 原文链接：https://blog.csdn.net/Vermont_/article/details/108690251
>
> 本文为原文核心内容归纳总结，仅用于个人学习存档，无任何商业用途，版权归原作者所有。